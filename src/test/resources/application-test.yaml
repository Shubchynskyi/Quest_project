spring:
  sql:
    init:
      mode: always
  liquibase:
    change-log: classpath:db/db.changelog-master.xml

valid:
  quest:
    text:
      path: validQuestText.txt
    name: "Quest name"
    description: "Quest description"
    userId: "1"
invalid:
  quest:
    text: "Invalid text"

app:
  images-directory: "src/test/resources/testImages"
  valid-quest-id: "1"
  invalid-quest-id: "999"
  test-image:
    name: "test-image.png"
    content-type: "image/png"
  localization:
    supported-languages: en, ru, ua, de
  tests:
    expectedQuestsSize: 1